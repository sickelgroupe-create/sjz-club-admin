# 管理后台部署

三端共用后端数据库，不需要为管理后台单独建数据库。
全部 SQL 统一在后端仓库 database/，Java 后端部署说明及服务器模板在 deploy/：https://github.com/sickelgroupe-create/sjz-club-backend/tree/main/deploy ，先阅读该目录的 README.md。

本目录是 Vue 3 / Vite 管理后台。使用 Node.js 22.12+（或兼容的更高版本）和 npm，执行 npm ci。
开发时设置自己的 .env.development，VITE_APP_BASE_API=/dev-api；开发代理见 vite.config.js，当前目标为 localhost:8082。

首次克隆时复制 .env.example 为 .env.development；生产构建前另复制一份为 .env.production，并确认：
- VITE_APP_TITLE：你自己的后台名称。
- VITE_APP_ENV：production。
- VITE_APP_BASE_API：/prod-api。
- VITE_APP_BASE_PATH：/（如果部署在子路径，需同步调整 Nginx）。

执行 npm run build:prod，将 dist 内容复制到服务器管理后台站点目录（模板为 /var/www/sjz-admin），不要上传 node_modules。
后端 deploy/nginx.conf.example 中 /prod-api/ 会转发到 127.0.0.1:8082/，注意保留两个路径末尾的斜杠。

管理员初始密码不随源码提供：按后端 deploy/README.md 在自己的新数据库激活 admin。
前端环境变量会进入公开浏览器代码，不能填写支付密钥、数据库密码或 AppSecret。
本地原有 .env 配置可能指向现网；构建新部署前必须核对并替换，不要误连线上。
